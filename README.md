# 📝 Django To-Do Application

A simple and clean To-Do web application built with **Django**.  
This project demonstrates CRUD operations (Create, Read, Update, Delete) using Django models, views, and templates.

---

## 🚀 Features
- Add new tasks
- View all tasks
- Update existing tasks
- Delete tasks
- Responsive UI with Django templates

---

## 🛠️ Tech Stack
- **Backend:** Django (Python)
- **Database:** SQLite (default, can be swapped with MySQL/PostgreSQL)
- **Frontend:** HTML, CSS, Bootstrap (via Django templates)

---

## 📂 Project Structure
